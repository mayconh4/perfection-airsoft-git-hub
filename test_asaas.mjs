import fetch from 'node-fetch';

const ASAAS_API_KEY = "$aact_YTU5YTE0M2M2N2I4MTliNDgwYjUzNDI2MTglMjYxMj"; // dummy or wait, do I have the Asaas API key locally?
// The edge function uses Deno.env.get('ASAAS_API_KEY'). But the user's .env file doesn't have it. It is set in Supabase Secrets. 
